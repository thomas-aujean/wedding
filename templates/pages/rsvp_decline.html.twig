{% extends "base.html.twig" %}
{% block body %}
	<section>
		<div class="title-img">
			<img src="{{ asset('images/flowers/black_eyed_susan_bouquet.png') }}" alt="">
		</div>

		<h1>{{ title|trans }}</h1>

		It's ok if your life is sad, we still love you !

		<div class="text-end">
			<a href="{{ path('reset', {'uuid': rsvp.uuid}) }}">J'ai changé d'avis !</a>
		</div>
	</section>
{% endblock %}
