<!DOCTYPE html>
<html>

	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}
				Annie & Thomas - 07/12/2025
			{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>"> {% block stylesheets %}{% endblock %}
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
		{% block javascripts %}
			{% block importmap %}
				{{ importmap('app') }}
			{% endblock %}
		{% endblock %}
	</head>

	<body>

		<nav class="navbar navbar-expand-lg bg-body-tertiary">
			<div class="container-fluid">
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNav">
					<ul class="navbar-nav">
						<li class="nav-item">
							<a class="nav-link {{ (app.request.pathinfo == path('home')) ? 'active' : ''}}" href="{{ path('home') }}">
								<span>H</span>OME
							</a>
						</li>

						<li class="nav-item">
							<a class="nav-link {{ (app.request.pathinfo == path('accomodations')) ? 'active' : ''}}" href="{{ path('accomodations') }}">
								<span>A</span>ccomodations
							</a>
						</li>

						<li class="nav-item">
							<a class="nav-link {{ (app.request.pathinfo == path('transportation')) ? 'active' : ''}}" href="{{ path('transportation') }}">
								<span>T</span>ransportation
							</a>
						</li>

						<li class="nav-item">
							<a class="nav-link {{ (app.request.pathinfo == path('itinerary')) ? 'active' : ''}}" href="{{ path('itinerary') }}">
								<span>I</span>tinerary
							</a>
						</li>

						<li class="nav-item">
							<a class="nav-link {{ (app.request.pathinfo == path('rsvp')) ? 'active' : ''}}" href="{{ path('rsvp') }}">
								<span>R</span>SVP
							</a>
						</li>

						<li class="nav-item">
							<a class="nav-link {{ (app.request.pathinfo == path('registry')) ? 'active' : ''}}" href="{{ path('registry') }}">
								<span>R</span>egistry
							</a>
						</li>

						<li class="nav-item">
						<div style="display:inline-flex; align-items:center">
					<a class="nav-link" href="">EN</a>/<a class="nav-link" href="">FR</a>
				</div>
						</li>
					</ul>
				</div>
			</div>
		</nav>


		<main>
			{% block body %}{% endblock %}
		</main>
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
	</body>

</html>
